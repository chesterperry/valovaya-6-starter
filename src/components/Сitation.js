import React from "react";
// import PropTypes from 'prop-types'

const Citation = ({ children }) => {
	return (

				<div class="container">
					<p class="is-size-5">{children}</p>
				</div>
	);
};

export default Citation;
